<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <link rel="icon" type="image/svg+xml" href="/Images/favicon.ico" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>SuperAdmin Email Verification Successful</title>
        <!-- Add Google Font -->
        <link
            href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap"
            rel="stylesheet"
        />
        <style>
            * {
                padding: 0;
                margin: 0;
                box-sizing: border-box;
            }
            body {
                width: 100vw;
                height: 100vh;
                display: flex;
                justify-content: center;
                align-items: center;
                background: linear-gradient(135deg, #3a1cb5, #1452c3);
                font-family: "Poppins", sans-serif;
                color: #333;
            }
            .container {
                text-align: center;
                background: #fff;
                padding: 2.5rem;
                border-radius: 12px;
                box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
                max-width: 400px;
                width: 90%;
                animation: fadeIn 1s ease-in-out;
            }
            h1 {
                color: #2c3e50;
                margin-bottom: 1rem;
                font-size: 2rem;
                font-weight: 600;
            }
            p {
                color: #555;
                margin-bottom: 1.5rem;
                font-size: 1rem;
                line-height: 1.6;
            }
            .icon {
                font-size: 4rem;
                color: #2980b9;
                margin-bottom: 1rem;
                animation: bounce 1s ease-in-out;
            }
            a {
                display: inline-block;
                padding: 0.75rem 1.5rem;
                color: #fff;
                background-color: #2980b9;
                text-decoration: none;
                border-radius: 6px;
                font-size: 1rem;
                transition: background-color 0.3s ease;
            }
            a:hover {
                background-color: #1f6aa5;
            }
            /* Animations */
            @keyframes fadeIn {
                from {
                    opacity: 0;
                    transform: translateY(20px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }
            @keyframes bounce {
                0%,
                20%,
                50%,
                80%,
                100% {
                    transform: translateY(0);
                }
                40% {
                    transform: translateY(-20px);
                }
                60% {
                    transform: translateY(-10px);
                }
            }
        </style>
    </head>
    <body>
        <div class="container">
            <!-- Checkmark Icon -->
            <h1 style="text-decoration: underline">HELLOMATES</h1>
            <div class="icon">✓</div>
            <h1>SuperAdmin Email Verified Successfully!</h1>
            <p>
                Your SuperAdmin email has been successfully verified. You can
                now log in to your SuperAdmin account and access all
                administrative features.
            </p>
        </div>

        <!-- Add the canvas-confetti library -->
        <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
        <script>
            document.addEventListener("DOMContentLoaded", function () {
                const duration = 2000; // 2 seconds
                const end = Date.now() + duration;

                (function frame() {
                    confetti({
                        particleCount: 5,
                        spread: 70,
                        origin: { y: 0.6 },
                    });

                    if (Date.now() < end) {
                        requestAnimationFrame(frame);
                    }
                })();
            });
        </script>
    </body>
</html>
